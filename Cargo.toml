[package]
name = "intcode-rs"
version = "0.1.0"
edition = "2021"
default-run = "intcode-rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
clap = { version = "3.1.18", features = ["derive"] }
